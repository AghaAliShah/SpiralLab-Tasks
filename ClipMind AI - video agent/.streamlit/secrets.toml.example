# Rename this file to "secrets.toml" for LOCAL streamlit runs, and paste your key.
# On Streamlit Community Cloud, do NOT commit secrets - paste this into the app's
# Settings -> Secrets box instead.
OPENROUTER_API_KEY = "sk-or-v1-your_key_here"
